version: 2.1

jobs:
  ci:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Check env"
          command: |
            uname -a
            df -h
            docker images
            env
      - run:
          name: "Build docker image"
          command: |
            docker buildx build -t android-build-box .

workflows:
  CI:
    jobs:
      - ci
